dependencies:

  cache_directories:
    - vendor
  override:
    - mkdir -p build/logs
    - mkdir -p build/cov
    - composer install --dev --no-interaction

test:
  override:
    - phpunit
  post:
      - bin/coveralls -v --exclude-no-stmt